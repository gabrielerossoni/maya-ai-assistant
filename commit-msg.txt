feat(core, ui, arduino): allarme 20s + sveglia affidabile + orb/work-mode stabile + TTS news/meteo migliorati + "buonanotte" blu

Motivazione
- Rendere affidabile la sveglia e l’allarme con stop certo a 20s.
- Evitare rimbalzi tra home (orb) e work-mode nella dashboard.
- Migliorare la qualità del parlato per meteo/notizie e "rientro".
- Ripristinare l’atmosfera notturna con luce blu soffusa.

Modifiche principali
- core/automation_engine.py
  - fix(lock): rilascio lock scena con try/finally per prevenire deadlock.
  - feat(scene: buongiorno): ritorno a layout 'orb' con supporto exitAfter.
  - feat(scene: buonanotte): luce blu soffusa (brightness 32, rgb 0,0,16).
  - feat(scene: sono rientrato): GET Arduino più robusto; dice sempre "Bentornato!"; stato casa più umano da sensori locali.
  - feat(TTS meteo): normalizzazione abbreviazioni (parzialm./parz., nuvol., poss., tempor., preval./prev.) con regex sicure.
  - feat(TTS news): frase con 1–2 titoli e rimozione " - Sorgente".
- main.py
  - feat(allarme/quake): auto-clear scena "allarme" dopo ~20.5s; broadcast 'scene_cleared'.
  - feat(bus→WS): inoltro 'scene_executed' alla dashboard (pill aggiornato anche per scheduler/eventi).
- tools/display_tool.py
  - feat(params): restituisce parametri top-level (es. exitAfter) per il frontend.
- static/maya_dashboard.html
  - fix(layout orb): nessun enterWorkMode quando arriva 'orb'; nav su 'idle'.
  - feat(state): 'currentLayout' + debounce orb (~1.2s) per evitare rimbalzi; gating work-mode su attività vocale solo se non in orb.
- tools/weather_tool.py
  - fix: timeout=4s chiamata Open-Meteo.
- tools/arduino_tool.py
  - fix(sim): gestione buzz2_playing coherente (off/stop → False).
- arduino/maya_controller/maya_controller.ino
  - fix(melodie): noTone() prima di cambiare; buffer statico per nome melodia; include <string.h>.
  - feat(neopixel): timeout sicurezza 20s per effetto alert.

Impatto
- Nessun breaking change; consigliato buzzer attivo su D8 con driver NPN per volume elevato.

Test
- Sveglia: melodia completa e ripetuta ~30s.
- Allarme: beep/sirena + neon, stop in 20s e pill cleared.
- Rientro: "Bentornato!" + stato umano (solo sensori Arduino).
- Buongiorno: news/meteo naturali; ritorno a orb; navigazione stabile.
